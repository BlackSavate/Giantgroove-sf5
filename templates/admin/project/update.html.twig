{% extends 'base.html.twig' %}

{% block title %}{{ 'edit'|trans|capitalize }} {{ project.title }}{% endblock %}

{% block breadcrumb %}
    <a href="{{ path('homepage') }}">{{ 'home'|trans|capitalize }}</a> &gt;
    <a href="{{ path('admin_dashboard_dashboard') }}">{{ 'admin'|trans|capitalize }}</a> &gt;
    <a href="{{ path('admin_project_list') }}">{{ 'projects'|trans|capitalize }}</a> &gt;
    <span class="bc-current">{{ project.title|capitalize }}</span>
{% endblock %}

{% block body %}
  {% if is_granted('IS_AUTHENTICATED_FULLY') %}
  {{ form_start(form) }}
      {{ form_widget(form) }}
      <input class="button btn btn-secondary" type="submit" value="{{ 'edit'|trans|capitalize }}" />
  {{ form_end(form) }}
  {% endif %}
{% endblock %}
